[project]
name = "dgp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datasets",
    "fasttext",
    "langgraph",
    "openai",
    "sacrebleu",
    "transformers",
    "unbabel-comet",
]
